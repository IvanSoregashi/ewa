[project]
name = "ewa"
version = "0.1.0"
description = "CLI helper"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ebooklib>=0.19",
    "typer-shell>=0.1.12",
]

[project.scripts]
ewa = "ewa.main:main"
