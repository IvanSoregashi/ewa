[project]
name = "ewa"
version = "0.1.0"
description = "CLI helper"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.9.0", # Typer's recommended dependency for better output
    "rich>=13.0.0",
]
